<%= simple_form_for([:admin, @location]) do |f| %>
  <div class="form-inputs">
    <%= f.input :location_name %>
    <%= f.input :location_id, label: "Location Id" %>
  </div>

  <div class="form-actions">
    <%= f.button :submit, class: "button small orange" %>
  </div>
<% end %>
